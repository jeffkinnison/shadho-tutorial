#!/usr/bin/env bash

# This file is similar to a script that would be submitted to a batch system
# like slurm, torque, etc. In it, we set up the envitonment for the job (e.g.
# loading modules, activating a virtualenv, setting environment variables,
# etc.).

python3 objective.py